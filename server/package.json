{
    "name": "filament-mat-server",
    "description": "Language Server for Filament Material files",
    "version": "0.0.1",
    "engines": {
        "node": "*"
    },
    "scripts": {
        "compile": "tsc -b",
        "watch": "tsc -b -w"
    },
    "dependencies": {
        "vscode-languageserver": "^9.0.0",
        "vscode-languageserver-textdocument": "^1.0.0"
    },
    "devDependencies": {
        "typescript": "^5.0.0"
    }
}
